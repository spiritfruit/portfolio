---
const links = [
  { href: '/', label: 'Home' },
];
---

<nav class="site-nav">
  <a class="site-nav__brand" href="/">Your Name</a>
  <ul class="site-nav__links">
    {links.map((link) => (
      <li><a href={link.href}>{link.label}</a></li>
    ))}
  </ul>
</nav>
